import { Typography } from '@mui/material';

function Header() {
  return (
    <header>
      <Typography variant="h1" color="primary">
        Our Store
      </Typography>
      <hr />
    </header>
  );
}

export default Header;
